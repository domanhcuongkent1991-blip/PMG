# WORKLOG 2026-05-01

## Scope
- Verify whether daily session memory/worklog is being maintained.
- Continue the recommended P0/P1 work:
  - P0: make Google Sheets auth depend on refresh token instead of short-lived access token.
  - P1: preserve confidence in two-way sync after the auth change.
- Use Superpowers workflow:
  - `using-superpowers`
  - `test-driven-development`
  - `verification-before-completion`

## Session Memory Check
1. Local worklogs found:
   - `WORKLOG_2026-04-22.md`
   - `WORKLOG_2026-04-23.md`
   - `WORKLOG_2026-04-24.md`
   - `WORKLOG_2026-04-26.md`
   - `WORKLOG_2026-04-28.md`
2. Missing daily worklogs for recent active days:
   - `2026-04-29`
   - `2026-04-30`
   - `2026-05-01` before this file was created.
3. MemPalace diary check:
   - Only old entries for another project were found.
   - No durable memory entry existed for this project before this session.
4. GSD/Codex health:
   - `scripts/global-health-check.ps1 -Json` returned `WARN`.
   - Reason: old Codex thread paths with extended prefix exist globally.
   - Current project had no mismatch/rollout-missing count in that check.
   - Decision: do not run GSD auto pipeline from this active session.

## Changes Made
1. Added test-covered auth mode selection:
   - `android-mvp/app/src/main/java/com/example/devicetracker/data/sheet/SheetsAuthMode.kt`
   - `android-mvp/app/src/test/java/com/example/devicetracker/data/sheet/SheetsAuthModeTest.kt`
2. Updated `SheetConfig` to expose auth mode and keep refresh-token preference explicit:
   - `android-mvp/app/src/main/java/com/example/devicetracker/data/sheet/SheetConfig.kt`
3. Extracted sync-friendly error messaging into a testable helper:
   - `android-mvp/app/src/main/java/com/example/devicetracker/ui/sync/SyncFriendlyMessage.kt`
   - `android-mvp/app/src/test/java/com/example/devicetracker/ui/sync/SyncFriendlyMessageTest.kt`
4. Updated the 401 auth message to guide operators toward OAuth refresh-token configuration instead of short-lived access-token replacement.
5. Updated debug build config:
   - If `SHEETS_REFRESH_TOKEN` is configured, `SHEETS_ACCESS_TOKEN` is intentionally embedded as an empty string.
   - This reduces dependence on the short-lived token and avoids carrying that extra secret in the debug APK.

## Verification Evidence
1. Targeted unit tests:
   - Command:
     - `./gradlew.bat -PcodexBuildId=p0-green :app:testDebugUnitTest --tests '*SheetsAuthModeTest' --tests '*SyncFriendlyMessageTest' --no-daemon --max-workers=1`
   - Result:
     - `BUILD SUCCESSFUL`
2. Full unit tests:
   - Command:
     - `./gradlew.bat -PcodexBuildId=p0p1-verify :app:testDebugUnitTest --no-daemon --max-workers=1`
   - Result:
     - `BUILD SUCCESSFUL`
3. Debug build:
   - Command:
     - `./gradlew.bat -PcodexBuildId=p0p1-build :app:assembleDebug --no-daemon --max-workers=1`
   - Result:
     - `BUILD SUCCESSFUL`
   - APK:
     - `android-mvp/.codex-build/p0p1-build/app/outputs/apk/debug/app-debug.apk`
4. Generated BuildConfig check:
   - `SHEETS_ACCESS_TOKEN` is empty.
   - `SHEETS_REFRESH_TOKEN` is configured.
   - `SHEETS_OAUTH_CLIENT_ID` is configured.
   - Secret values were not printed.
5. OAuth refresh-token smoke check:
   - Used local OAuth config without printing secrets.
   - Refreshed an access token successfully.
   - Read Google Sheet metadata successfully.
   - Sheet title: `beta 2`.
   - Required tabs included:
     - `1607125070:DMBT 2026`
     - `57428884:HGT định kỳ`
6. Device install:
   - Installed new APK successfully.
   - Package `com.example.devicetracker` last update time became `2026-05-01 18:50:28`.
7. Manual full sync after user tapped the sync button:
   - `DeviceLogRepository`: `refreshFromRemote success: fetched=128, applied=128, skipped=0`
   - `HgtCheckRepository`: `refreshFromRemote success: fetched=49, applied=26, skipped=23`
   - `SyncStatusViewModel`: `syncNow success mode=FULL elapsedMs=4257`
   - UI state after sync:
     - Total local records: `2044`
     - Synced records: `2044`
     - Pending records: `0`
     - Queue items: `0`
     - Retry error items: `0`

## Notes / Risks
- Gradle/Kotlin daemon still logs temp-file access warnings, but Gradle falls back and exits successfully.
- ADB briefly lost device visibility after daemon restart, then recovered.
- Manual full-sync verification was completed after the user tapped the sync button.
- The most important auth verification was completed: APK no longer embeds access token, refresh-token API flow works, and the installed app completed full sync without 401/403.

## Next Recommended Work
1. Add an operator-safe script to create/update daily worklogs automatically.
2. Add a non-secret Google Sheets config verification script for future token/sheet changes.
3. Add UI-level sync status split between DMBT and HGT counts.
4. Add automated/instrumented sync smoke test if a stable test fixture sheet is available.
- 2026-05-01 18:56:32 - Continue P0/P1 operational hardening and sync visibility work.
- 2026-05-01 19:16:20 - Implemented sync status DMBT/HGT breakdown. Verified targeted SyncOverviewTest, full debug unit tests, assembleDebug, ops:test, and offline Sheets config. adb install blocked by sandbox approval quota; APK is built at android-mvp\.codex-build\split-build\app\outputs\apk\debug\app-debug.apk.
- 2026-05-01 20:05:59 - Implemented safe multi-sheet sync foundation: SheetSyncRegistry, future sheet contracts, SheetDryRunValidator, sheets inventory script, ops:sheets-inventory, sheets inventory doc, and multi-sheet safety plan. Verified ops:test, offline inventory, offline Sheets config, full debug unit tests, and assembleDebug. Online inventory still blocked by PowerShell HTTPS receive error.
