# WORKLOG 2026-05-08

## Workflow Pilot - Code Review Only

### Goal

Run the next safe step after upgrading F:\CODEX-AUTO: a controlled workflow pilot on this Android project without editing Android source code.

### What Was Done

- Read `input/prd.md` and confirmed the project still prioritizes local-first Android + Google Sheets sync, no data loss, no wrong sheet/wrong row, no duplicates, and evidence-based validation.
- Ran required preflight commands for Node, npm, npx, and git.
- Created a GSD-style pilot phase: `.planning/phases/2026-05-08-workflow-pilot-code-review-only`.
- Classified the pilot as `workflow_task_type: code_review_only`.
- Produced review, code quality, no-source-edits, summary, and verification artifacts.

### Files/Reports Created

- `.planning/phases/2026-05-08-workflow-pilot-code-review-only/WORKFLOW-PILOT-PLAN.md`
- `.planning/phases/2026-05-08-workflow-pilot-code-review-only/WORKFLOW-PILOT-REVIEW.md`
- `.planning/phases/2026-05-08-workflow-pilot-code-review-only/CODE_QUALITY_REPORT.md`
- `.planning/phases/2026-05-08-workflow-pilot-code-review-only/NO_SOURCE_EDITS.md`
- `.planning/phases/2026-05-08-workflow-pilot-code-review-only/WORKFLOW-PILOT-SUMMARY.md`
- `.planning/phases/2026-05-08-workflow-pilot-code-review-only/WORKFLOW-PILOT-VERIFICATION.md`

### Important Boundaries

- No Android source code edits were intentionally made by this pilot.
- No Google Sheet writes.
- No APK install.
- No deploy/release approval.

### Remaining Risk

- The repo had many pre-existing uncommitted source changes before this pilot.
- Real build/test/UAT still needs to run before claiming app behavior is safe.
- Existing hard-coded monthly sheet classification should be handled in a narrow future phase.

### Next Step

Run a narrow GSD-supervised phase for one concrete task, preferably either code_cleanup for configuration maintainability or bug_fix with fail-first reproduction.

## Workflow Pilot Verification Result

### Commands Run

- `node scripts/prevent-secrets.js` -> PASS, exit code 0.
- `powershell -NoProfile -ExecutionPolicy Bypass -File F:\CODEX-AUTO\WORKFLOW-ORCHESTRATOR\scripts\Test-WorkflowArtifacts.ps1 -ProjectPath F:\codex_android_gsheet_full_pack` -> FAIL at governance approval/order level.

### What Passed

- The new pilot phase has plan, summary, verification, review, code quality report, and no-source-edits evidence.
- The upgraded code-supervision gate recognized `code_quality_evidence_present: true`.
- The upgraded code-supervision gate recognized `no_source_edits_evidence_present: true`.

### What Is Still Blocked

- Level 1/2 workflow approval state for this repo is not complete under the upgraded gate.
- The pilot phase itself is not officially approved in state.json and approval-ledger.jsonl.
- Local `scripts/approve-phase-plan.ps1` still writes old-style phase approval ledger entries, while the upgraded gate expects `phase_approval` with `phase_id`.

### Decision

Do not start broad AI source-code edits yet. Normalize workflow approval evidence first, then run one narrow GSD-supervised code task.

## Workflow Schema Normalization

### Goal

Address the owner's review finding: local helper approval schema and upgraded F:\CODEX-AUTO gate schema must match so workflow failures are understandable.

### Changes Made

- Updated local phase approval helper to write upgraded ledger schema: `phase_approval` + `phase_id`.
- Updated local workflow-order gate to require valid phase approval ledger evidence, not only state.json.
- Added a workflow governance test that fails when the helper writes the old phase approval schema.
- Added `planning/13_WORKFLOW_HANDSHAKE.json` for upgraded Level 1 compatibility.
- Rewrote `SKILL_SELECTION_REPORT.md` with the exact required Level 2 sections.
- Added minimal `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` for upgraded Level 4 compatibility.
- Added summary/verification artifacts for the prior workflow governance phase so old phase state is explainable.

### Safety Boundary

No Android product source code was intentionally edited in this normalization step.

## Final Workflow Stabilization Verification

### Final Result

The workflow schema normalization is complete for the current controlled scope.

### Final Commands

- `node scripts/prevent-secrets.js` -> PASS.
- `npm run workflow:test` -> PASS.
- `npm run workflow:check` -> PASS.
- `powershell -NoProfile -ExecutionPolicy Bypass -File F:\CODEX-AUTO\WORKFLOW-ORCHESTRATOR\scripts\Test-WorkflowArtifacts.ps1 -ProjectPath F:\codex_android_gsheet_full_pack` -> PASS.

### Upgraded Gate Result

- status: PASS
- highest_continuous_level: 5
- rule_violations: none
- phase `2026-05-06-workflow-governance-hardening`: approval_valid true
- phase `2026-05-08-workflow-pilot-code-review-only`: approval_valid true, code_quality_evidence_present true, no_source_edits_evidence_present true

### Decision

Workflow can now be considered approximately 9/10 for controlled workflow governance. It is suitable for supervising the next narrow GSD/code task, but it still does not prove Android runtime behavior, Google Sheets UAT, or production readiness.

## Runtime Follow-up: Intermittent White Screen After Save

### User Feedback

- App now syncs 2-way between Google Sheet and app.
- Intermittent white screen still appears after saving and moving back toward sync/main flow.

### Manager Review + Agent Evidence

- `docs/uat/results/AGENT1_UAT_MULTIYEAR_POST_CONFIG_2026-05-07.md` shows multiyear sync PASS:
  - 2022..2026 counts > 0
  - `NULL sourceSheetId = 0`
  - sync2 idempotent.
- `docs/review/AGENT2_REVIEW_MULTIYEAR_POST_CONFIG_2026-05-07.md` confirms evidence consistency.
- Reviewed current code path around save/navigation/search refresh.

### Risk-Hardening Changes Applied

- Added save re-entry guard and failure handling in edit flow:
  - `android-mvp/app/src/main/java/com/example/devicetracker/ui/edit/EditUiState.kt`
  - `android-mvp/app/src/main/java/com/example/devicetracker/ui/edit/EditViewModel.kt`
  - `android-mvp/app/src/main/java/com/example/devicetracker/ui/edit/EditLogScreen.kt`
- Added defensive catch/log in list presentation pipeline to avoid silent UI collapse on unexpected mapper/filter errors:
  - `android-mvp/app/src/main/java/com/example/devicetracker/ui/search/SearchViewModel.kt`

### Build Verification

- Ran:
  - `.\scripts\build-android-safe.ps1 -SkipTests`
- Result:
  - `BUILD SUCCESSFUL`
  - Latest safe build output under `.codex-build` updated.

### Notes

- P1 finding on `sourceSheetId` backfill remains present as fixed logic:
  - `DeviceLogRepositoryImpl.kt` keeps `shouldBackfillSourceSheetIdOnly(...)` guard path.
- White-screen issue still needs targeted runtime reproduction with logcat evidence to confirm final root cause (this pass is defensive hardening, not final closure).

## Android Separate Safe Build

### Goal

Create a project-suitable Android debug build using the repo's isolated safe build pipeline. This build is separate from deploy/UAT and does not install APK on a device.

### Command Run

- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build-android-safe.ps1`

### Result

- `testDebugUnitTest` -> PASS (`BUILD SUCCESSFUL`, 33 tasks executed).
- `assembleDebug` -> PASS (`BUILD SUCCESSFUL`, 42 tasks: 18 executed, 24 up-to-date).
- Build pipeline completed successfully.

### APK Output

- Path: `android-mvp/.codex-build/1778249689304/app/outputs/apk/debug/app-debug.apk`
- Size: `23,521,870` bytes
- LastWriteTime: `2026-05-08 21:20:36`
- SHA256: `0722cddf73e4b203e031beef5f6ee397c06de05ed878edc7526f35f379f0c715`

### Warnings Observed

- Android metrics warning about `C:\Users\CodexSandboxOnline\.android\analytics.settings`; build still passed.
- Kotlin annotation default-target warnings in several files; not build-blocking.
- Deprecated `hiltViewModel` and `Icons.Filled.ArrowBack` warnings; not build-blocking.
- Native strip warning for `libandroidx.graphics.path.so`; APK was packaged successfully.

### Safety Boundary

- No APK install was performed.
- No Google Sheet write was performed.
- No deploy/release approval is implied by this build.

## Manager Review After Agent Completion (White-Screen Round 2)

### Context

- Agent 1 and Agent 2 reported completion for the white-screen investigation/fix cycle.
- User still observed intermittent blank/white screen after Save -> Back -> Sync navigation.

### Manager Verification

- Reviewed evidence/report pair:
  - `docs/uat/results/AGENT1_WHITE_SCREEN_RCA_2026-05-08.md`
  - `docs/review/AGENT2_WHITE_SCREEN_RCA_REVIEW_2026-05-08.md`
- Conclusion from evidence: issue remains reproducible as runtime freeze/stall; not proven fixed.
- Verified `sourceSheetId` backfill finding path in `DeviceLogRepositoryImpl.kt` is present:
  - `shouldBackfillSourceSheetIdOnly(...)` is checked before timestamp/content fallback.

### Additional Hardening Applied (Manager)

- Moved search/filter/sort presentation computation off main thread and added stale-job cancellation guards:
  - `android-mvp/app/src/main/java/com/example/devicetracker/ui/search/SearchViewModel.kt`
- Added `collectLatest` for list and timeline streams to reduce backlog pressure.

### Verification Commands

- `.\scripts\build-android-safe.ps1 -SkipTests` -> PASS (`BUILD SUCCESSFUL`).
- Attempted targeted Gradle tests in current environment:
  - `:app:testDebugUnitTest --tests DeviceLogRepositorySyncRulesTest --tests CategoryFilterMapperTest`
  - blocked by environment filesystem permission (`AccessDenied` under `app/build/generated/source/buildConfig/...`).

### Remaining Risks

- Runtime freeze still needs real-device measurement to prove frequency reduction after latest hardening.
- Current environment may not reliably execute direct Gradle test target path due build directory lock/permission behavior.

### Next-Step Policy

- Treat current status as "controlled UAT only", not production closure.
- Next cycle must separate:
  1. Runtime mitigation code path (single owner)
  2. Repro/proof instrumentation and measurement (independent reviewer)

## Manager Review After Agent 1 and Agent 2 Completion (Navigation Guard Fix)

### Context

- Agent 2 produced `docs/review/AGENT2_WS_VERIFY_02_RUNTIME_STABILITY_2026-05-08.md`.
- Verdict remains runtime stability FAIL: white screen/freeze is still reproduced and skipped-frame bursts remain high.

### Manager Finding

- Reviewed the UI navigation guard in `SearchScreen.kt`.
- Found that `navigationDispatched` was stored with `rememberSaveable` and was not reset when returning to the search screen.
- Risk: after opening detail/sync/add and coming back, later taps could be ignored, making the app look frozen even when it is still alive.

### Fix Applied

- Updated `SearchScreen.kt` to reset `navigationDispatched` on lifecycle `ON_RESUME`.
- Scope stayed within UI search navigation only.
- No sync core, repository, sheet config, DB schema, or API contract was changed.

### Verification

- Ran `.\scripts\build-android-safe.ps1 -SkipTests`.
- Result: `BUILD SUCCESSFUL`.
- Ran full `.\scripts\build-android-safe.ps1`.
- Result: `testDebugUnitTest` PASS and `assembleDebug` PASS.

### Decision

- Navigation guard bug: fixed and build-verified.
- Runtime white-screen/freeze: not approved as fully fixed yet.
- Next work should focus on measured runtime mitigation and stronger real-device evidence.

## Manager APK Install Verification

### Goal

Install the latest debug APK after Agent 1 `WS-FIX-02` and verify the app is present and launchable on the connected Android device.

### Device

- Serial: `1a79dec0`
- Model: `RMX3081`
- User verified in foreground evidence: `0`

### APK

- Path: `android-mvp/.codex-build/1778254298285/app/outputs/apk/debug/app-debug.apk`
- SHA256: `48397E441603FAE4AC960C6E6B5E3B0CDEA075ED0B352557242B98EB9BA06D58`

### Commands And Results

- `adb -s 1a79dec0 install -r <apk>` -> `Success`.
- `adb -s 1a79dec0 shell pm list packages | rg "com.example.devicetracker"` -> package present.
- `adb -s 1a79dec0 shell cmd package resolve-activity --brief com.example.devicetracker` -> `com.example.devicetracker/.MainActivity`.
- `adb -s 1a79dec0 shell am start -n com.example.devicetracker/.MainActivity` -> started.
- `dumpsys activity activities` -> `mResumedActivity` is `com.example.devicetracker/.MainActivity` on user `0`.

### Evidence

- Install report: `docs/uat/results/MANAGER_APK_INSTALL_RESULT_2026-05-08.md`
- Screenshot: `docs/uat/evidence/installed-apk-check-2026-05-08/launch_after_install.png`

### Decision

- Install and launch verification: PASS.
- This does not close the white-screen issue; runtime loop UAT is still required.

## Manager White Screen Live RCA And Navigation Fix

### User Feedback

- User reported the device still had the white-screen issue after the latest installed build.

### Evidence Captured

- `docs/uat/evidence/white-screen-live-2026-05-08/current_screen_pulled.png`
- `docs/uat/evidence/white-screen-live-2026-05-08/dumpsys_activity_activities.txt`
- `docs/uat/evidence/white-screen-live-2026-05-08/dumpsys_window.txt`
- `docs/uat/evidence/white-screen-live-2026-05-08/gfxinfo.txt`
- `docs/uat/evidence/white-screen-live-2026-05-08/logcat_dump.txt`
- `docs/uat/evidence/white-screen-live-2026-05-08/ui_dump.xml`
- `docs/uat/evidence/white-screen-live-2026-05-08/after_force_stop_relaunch.png`

### RCA

- App was foreground/resumed/focused, but Compose content under `android:id/content` was empty.
- No ANR and no fatal app crash were found.
- Force-stop and cold relaunch restored the normal search screen.
- Most likely cause: navigation back stack was popped past the Search root during rapid/double back flow, leaving `NavHost` with no active destination to draw.

### Fix

- Updated `android-mvp/app/src/main/java/com/example/devicetracker/ui/navigation/DeviceTrackerNavGraph.kt`.
- Replaced direct `popBackStack()` callbacks with `safePopBackStack()`.
- `safePopBackStack()` refuses to pop the Search root and falls back to Search if navigation state becomes empty.

### Verification

- Ran `.\scripts\build-android-safe.ps1 -SkipTests`.
- Result: `BUILD SUCCESSFUL`.
- APK: `android-mvp/.codex-build/1778257277724/app/outputs/apk/debug/app-debug.apk`
- SHA256: `846228FD53AD9F2F99B50421849E44C4575A617BFA2C3590E61A78569B1A01B0`

### Blocker

- Final reinstall was blocked because ADB no longer listed device `1a79dec0`.

### Decision

- Code fix is build-verified.
- Runtime closure is pending reinstall and real-device loop retest.

### Follow-up Install After Device Reconnect

- Device `1a79dec0` was reconnected.
- Installed APK: `android-mvp/.codex-build/1778257277724/app/outputs/apk/debug/app-debug.apk`.
- APK SHA256: `846228FD53AD9F2F99B50421849E44C4575A617BFA2C3590E61A78569B1A01B0`.
- Install result: `Success`.
- Package check: `com.example.devicetracker` installed and resolves to `com.example.devicetracker/.MainActivity`.
- Package metadata showed `lastUpdateTime=2026-05-08 23:30:30`.
- Cold launch after force-stop: PASS; `MainActivity` was resumed, visible, and focused.
- Evidence screenshot: `docs/uat/evidence/white-screen-nav-fix-install-2026-05-08/launch_after_nav_fix_install.png`.
- Result report: `docs/uat/results/MANAGER_NAV_FIX_INSTALL_RESULT_2026-05-08.md`.

### Updated Decision

- Fixed APK installation on the real device: PASS.
- Cold launch on the real device: PASS.
- User follow-up UAT: PASS; after testing, the user no longer observed the white-screen issue.
- Original intermittent white-screen defect is approved as fixed for current UAT scope.
- Residual note: continue monitoring during longer continuous-use sessions because the original issue was intermittent.

## GSD Artifact Cleanliness Policy Upgrade

### Goal

Reduce repo noise caused by GSD/runtime artifacts without hiding real Android source changes.

### Changes Made

- Added `governance/ARTIFACT_POLICY.md` to define:
  - product source,
  - governance evidence,
  - runtime/raw evidence.
- Added `scripts/check-gsd-artifacts.ps1` to classify dirty repo state into:
  - source changes,
  - governance artifacts,
  - review/UAT reports,
  - raw evidence,
  - workflow scripts,
  - other dirty files.
- Added `scripts/test-workflow-clean-check.ps1` with a temp git repo fixture.
- Added npm commands:
  - `npm run workflow:clean-check`
  - `npm run workflow:clean-test`
- Updated `.gitignore` so raw runtime evidence under `docs/uat/evidence/**` is ignored deeply.
- Kept Android source/test files visible so product changes cannot be hidden by cleanup rules.

### Verification

- `npm run workflow:clean-test` -> PASS.
- `npm run workflow:clean-check` -> FLAG, not FAIL:
  - `raw_evidence`: 0
  - `source_changes`: 30
  - `governance_artifacts`: 28
  - `review_and_uat_reports`: 57
  - `workflow_scripts`: 11
  - `other_dirty`: 0
- `git status --short --untracked-files=all | Select-String -Pattern 'docs/uat/evidence|\.db|\.png|logcat|\.zip'` -> no output.
- `node scripts/prevent-secrets.js` -> PASS.
- `git diff --check -- .gitignore package.json scripts/check-gsd-artifacts.ps1 scripts/test-workflow-clean-check.ps1 governance/ARTIFACT_POLICY.md WORKLOG_2026-05-08.md` -> PASS, only LF/CRLF warnings.
- Full `git diff --check` still reports pre-existing product-source whitespace in `android-mvp/app/src/main/java/com/example/devicetracker/ui/search/SearchScreen.kt`; not changed in this policy task.

### Decision

This is the intended state after the policy upgrade:

- Raw UAT/runtime evidence no longer pollutes Git status.
- Real source changes remain visible and must be reviewed/committed separately.
- Governance/report artifacts remain visible until the owner decides to commit or archive them.

## Manager Acting As Agent 2 - Fast Track UAT Review

### Goal

Review Agent 1 fast-track DMBT 2025/2026 2-way UAT result, then return to Managerial decision-making.

### Inputs Reviewed

- `docs/uat/results/AGENT1_FAST_UAT_DMBT_2025_2026_2WAY_2026-05-08.md`
- `docs/uat/evidence/fast-uat-dmbt-2025-2026-2026-05-08/preflight_output.txt`
- `docs/uat/results/AGENT1_UAT_MULTIYEAR_POST_CONFIG_2026-05-07.md`
- `docs/review/AGENT2_REVIEW_MULTIYEAR_POST_CONFIG_2026-05-07.md`

### Agent 2 Reports Created

- `docs/review/AGENT2_FAST_REVIEW_PREP_STATIC_EVIDENCE_2026-05-08.md`
- `docs/review/AGENT2_FAST_REVIEW_A1_DMBT_2025_2026_2026-05-08.md`

### Findings

- Agent 1 stayed within scope and did not report source/config/schema changes.
- Agent 1 correctly marked the fast-track 2-way UAT as `BLOCKED` because `adb devices -l` returned no connected device.
- Existing 2026-05-07 evidence still supports DMBT 2022-2026 pull/count/idempotency:
  - `total_device_logs = 1852`
  - `NULL sourceSheetId = 0`
  - `duplicate_groups = 17`
  - `rows_in_duplicate_groups = 34`
  - DMBT 2025 gid `989601207 = 558`
  - DMBT 2026 gid `1607125070 = 145`
- Existing evidence does not replace the missing real-device 2-way write-back proof for DMBT 2025/2026.

### Decision

- Agent 1 execution quality: PASS.
- Fast-track 2-way UAT result: BLOCKED, not failed.
- Managerial decision: do not approve the 2025/2026 2-way sync gate until the device is reconnected and the minimal blocked UAT is rerun.

## Manager Fast UAT Rerun Prep After Device Reconnect

### Goal

Resume the previously blocked DMBT 2025/2026 fast-track 2-way UAT after the Director reconnected the phone.

### Results

- `adb devices -l` showed device `1a79dec0` connected.
- Package `com.example.devicetracker` is installed.
- App resolves to `com.example.devicetracker/.MainActivity`.
- App launched successfully and `MainActivity` was resumed/focused.
- Captured screenshots/UI dumps and a before-sync DB snapshot in:
  - `docs/uat/evidence/fast-uat-dmbt-2025-2026-rerun-2026-05-09/`
- Result report:
  - `docs/uat/results/MANAGER_FAST_UAT_DMBT_2025_2026_RERUN_PREP_2026-05-09.md`

### Baseline Before Sync

- `total_device_logs = 1852`
- `NULL sourceSheetId = 0`
- DMBT 2025 gid `989601207 = 558`
- DMBT 2026 gid `1607125070 = 145`
- `duplicate_groups = 17`
- `rows_in_duplicate_groups = 34`
- `syncStatus PENDING = 5`
- `syncStatus SYNCED = 1847`

### Risk / Decision Needed

- The 5 pending rows are all `UPSERT_LOG` rows targeting DMBT 2026 gid `1607125070`.
- Pressing `Đồng bộ đầy đủ` may write those 5 pending records to the real Google Sheet.
- Manager stopped before pressing sync because this is a real Sheet write decision.

## Manager Fast UAT - Local Create Provenance Finding

### Goal

Execute Director-approved option 2: prepare a DMBT 2025 marker before syncing, so the UAT can cover both DMBT 2025 and DMBT 2026.

### Finding

- A DMBT 2025 marker record was created through the installed app:
  - `maThietBi = CODEX2025B`
  - `ngayPhatHien = 09/05/2025`
  - `recordId = DMBT-68713fdf-01f8-475b-b793-a206759c4f2e`
- DB inspection showed this new local record was `PENDING` but had `sourceSheetId = NULL`.
- This is a P1 blocker for safe multi-year App -> Sheet sync because the push layer requires an unambiguous target sheet when multiple DMBT sheets exist.

### Fix Patch Applied

- `android-mvp/app/src/main/java/com/example/devicetracker/data/sheet/SheetConfig.kt`
- `android-mvp/app/src/main/java/com/example/devicetracker/ui/edit/EditViewModel.kt`
- `android-mvp/app/src/main/java/com/example/devicetracker/data/repository/DeviceLogRepositoryImpl.kt`
- `android-mvp/app/src/test/java/com/example/devicetracker/data/sheet/SheetConfigMappingRulesTest.kt`
- Report: `docs/review/MANAGER_FAST_UAT_LOCAL_CREATE_PROVENANCE_FIX_2026-05-09.md`

### Verification Status

- Targeted Gradle test attempt timed out after 240s due Kotlin daemon / filesystem access issues.
- `scripts/build-android-safe.ps1 -SkipTests` was started but stopped by the Director because it was taking too long.
- Lingering Java build processes were stopped.

### Decision

- Do not press `Đồng bộ đầy đủ` on the currently installed APK yet.
- The installed APK does not include the provenance patch.
- The pending `CODEX2025B` marker remains a test record requiring either patched APK install/backfill or intentional cleanup before production sync closure.

## 2026-05-09 - Manager fast UAT DMBT 2025 provenance and isolated pending sync

### Muc tieu
- Day nhanh UAT tren dien thoai that cho DMBT 2025/2026 hai chieu.
- Sua rui ro local-created DMBT khong co `sourceSheetId`, gay push sai/khong push duoc khi app co nhieu sheet DMBT theo nam.
- Khong xoa data tren thiet bi neu chua can thiet.

### Viec da lam
- Phat hien APK moi ban dau bi Android tu choi cap nhat do khac signing key voi app dang cai.
- Keo `installed-base.apk` tu thiet bi, so fingerprint bang `apksigner`, xac nhan app dang cai dung key `8124cd8faaecac25253f01e6a5de53ba8f53554c9351d5a6a532875737c81900`.
- Build lai debug APK bang `android-mvp/.android-home/debug.keystore` de giu nguyen data khi `adb install -r`.
- Cai de thanh cong; `firstInstallTime` giu nguyen, `lastUpdateTime=2026-05-09 19:16:37`.
- Them mapping ngay phat hien -> DMBT yearly sheet id.
- Them backfill `sourceSheetId` cho pending legacy row truoc push.
- Them isolated fallback: neu batch push fail vi `Ambiguous DMBT fallback key`, app retry tung record, sync record hop le va chi giu lai record loi.

### Ket qua UAT that
- Marker local `CODEX2025B`, ngay phat hien `09/05/2025`, duoc backfill `sourceSheetId=989601207` va chuyen `SYNCED`.
- Dong bo day du bao: hang doi `6->1`, pending `6->1`, thoi gian khoang 25s.
- Logcat xac nhan:
  - `syncPending ambiguous fallback isolated: successful=5, failed=1`
  - `syncPending success: queueDeleted=5, markedSynced=5, failed=1`
  - `syncNow success mode=FULL elapsedMs=25170`
- Con 1 pending historical/dirty row: `dmbt-auto-e2elo998-25_06_2026-jsjsjjss-jsjsjjssjnsnsn`, loi ambiguous fallback. Day la data cleanup, khong con chan sync cac record hop le.

### Lenh verify da chay
- `./gradlew.bat --no-daemon :app:assembleDebug` voi `ANDROID_USER_HOME=android-mvp/.android-home`.
- `apksigner verify --print-certs android-mvp/app/build/outputs/apk/debug/app-debug.apk`.
- `adb -s 1a79dec0 install -r android-mvp/app/build/outputs/apk/debug/app-debug.apk`.
- `adb shell monkey -p com.example.devicetracker -c android.intent.category.LAUNCHER 1`.
- `adb logcat`, `uiautomator dump`, DB pull qua `run-as cat` cho `device_tracker.db`, `wal`, `shm`.

### Bao cao/evidence
- `docs/review/MANAGER_FAST_UAT_DMBT_2025_PROVENANCE_AND_ISOLATED_SYNC_2026-05-09.md`
- `docs/uat/evidence/fast-uat-dmbt-2025-2026-rerun-2026-05-09/after_isolated_sync_logcat.txt`
- `docs/uat/evidence/fast-uat-dmbt-2025-2026-rerun-2026-05-09/after_isolated_sync_screen.png`
- `docs/uat/evidence/fast-uat-dmbt-2025-2026-rerun-2026-05-09/after_isolated_sync_read.db`

### Rui ro con lai
- Con 1 pending ambiguous row can cong cu cleanup/ignore co xac nhan cua Giam doc.
- Chua chay full test suite do uu tien fast UAT; da co build success va real-device UAT pass co dieu kien.
