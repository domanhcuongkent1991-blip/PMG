# Worklog 2026-05-04

## Muc tieu
- Buoc 1: Kiem tra cac loi hay bi lap lai va fix triet de trong pham vi co the sua bang code.
- Buoc 2: Tiep tuc on dinh sync DMBT bang test:
  - Pull nhieu sheet giu dung `sourceSheetId`.
  - Push retry/payload lap khong tao duplicate row hang loat.

## Loi lap lai da xu ly

### `prevent-secrets.js` skip thanh cong khi Node khong goi duoc git
- Hien tuong cu:
  - Trong sandbox, `node scripts\prevent-secrets.js` in `Not inside a git repository. Secret guard skipped.`
  - Exit code thanh cong, de gay hieu nham la guard da quet xong.
- Nguyen nhan:
  - Node `child_process` bi chan khi spawn `cmd.exe`: `spawnSync C:\Windows\system32\cmd.exe EPERM`.
  - Script bat loi nay va coi nhu khong o trong Git repo.
- Fix:
  - `scripts/prevent-secrets.js` nay fail ro neu khong kiem tra duoc Git repo.
  - Thong diep moi: `Secret guard could not verify staged files... Commit must not continue until this guard runs successfully.`
- Ket qua:
  - Trong sandbox: fail ro, khong skip thanh cong nua.
  - Truoc commit that van can chay lai trong terminal binh thuong co quyen goi `git`.

### Gradle/Windows build lock
- Loi cu:
  - `android-mvp/app/build` bi Windows khoa file build output.
  - Da workaround truoc do bang rename build folder va build lai thanh cong.
- Trang thai hien tai:
  - User da xoa `android-mvp/app/build.locked-old` bang quyen admin.
  - Test/build hien tai pass.

## Sync stability da them

### Pull DMBT nhieu sheet giu `sourceSheetId`
- Them helper `dmbtPulledRecordIdentity(...)`.
- `pullDmbtLogsFromSheet(...)` dung helper nay de tao:
  - `recordId` local dung namespace khi can.
  - `sourceSheetId` dung sheet dang pull.
- Them test:
  - `dmbtPulledRecordIdentity_preservesSourceSheetIdForMultipleSheets`.

### Push retry/payload lap khong duplicate
- Them helper `dedupeDmbtLogsForPush(...)`.
- `pushLogsToDmbtSheet(...)` nay dedupe log truoc khi update/append.
- Rule:
  - Cung target sheet + cung sheet record id thi chi giu ban co `updatedAt` moi nhat.
  - Giup tranh truong hop queue/payload lap cung ban ghi lam append trung sau retry.
- Them test:
  - `dedupeDmbtLogsForPush_keepsLatestLogPerTargetSheetRecordId`.

## Verify
- Targeted test:
  - `android-mvp\gradlew.bat "-Dkotlin.compiler.execution.strategy=in-process" -p android-mvp testDebugUnitTest --tests com.example.devicetracker.data.remote.SheetsRemoteDataSourceRecordIdTest --no-daemon`
  - PASS / `BUILD SUCCESSFUL`.
- Full unit test:
  - `android-mvp\gradlew.bat "-Dkotlin.compiler.execution.strategy=in-process" -p android-mvp testDebugUnitTest --no-daemon`
  - PASS / `BUILD SUCCESSFUL`.
- Debug build:
  - `android-mvp\gradlew.bat "-Dkotlin.compiler.execution.strategy=in-process" -p android-mvp assembleDebug --no-daemon`
  - PASS / `BUILD SUCCESSFUL`.

## Secret scan
- `node scripts\prevent-secrets.js`
  - Fail ro trong sandbox voi `spawnSync C:\Windows\system32\cmd.exe EPERM`.
  - Day la hanh vi dung hon truoc: khong cho hieu nham la guard da pass.
- Quet thu cong file vua cham:
  - Chi thay ten bien/code auth san co trong `SheetsRemoteDataSource.kt`.
  - Khong thay token/secret that.

## Buoc tiep theo
- Chay dry-run/kiem tra target sheetId truoc khi ghi Google Sheet that.
- Kiem tra header that cua sheet `Sua chua T4.2026`, chi doc metadata/header, chua ghi.
- Viet mapper repair sheet row <-> repair update.

## Repair sheet mapper

### Muc tieu
- Chuan bi nen an toan cho sheet `Sua chua T4.2026` hai chieu.
- Chua ghi du lieu that len Google Sheet.

### Code da sua
- `android-mvp/app/src/main/java/com/example/devicetracker/data/model/SheetValueMappers.kt`
  - Them `DmbtRepairUpdate`.
  - Them `DmbtRepairUpdate.toDmbtRepairLogRow()`.
  - Them `Map<String, String>.toDmbtRepairUpdateFromRow()`.
  - Validation:
    - thieu cot bat buoc thi fail ro.
    - `record_id` khong duoc trong.
    - `ma_thiet_bi` khong duoc trong.
    - `updated_at` phai parse duoc Long.
    - `ngay_sua_chua` trong thi map ve null.
- `android-mvp/app/src/main/java/com/example/devicetracker/data/sheet/SheetContract.kt`
  - Them `ghi_chu` vao required columns cua `DMBT_REPAIR_LOG`.

### Test da them/sua
- `SheetValueMappersTest`
  - Test repair update -> sheet row.
  - Test blank repair date -> null.
  - Test thieu `ghi_chu` fail ro.
  - Test `updated_at` sai format fail ro.
- `RepairSheetContractTest`
  - Kiem tra `ghi_chu` la cot bat buoc.
  - Doi ten test opt-in cho dung trang thai hien tai.

### Verify bo sung
- Targeted repair mapper/contract tests:
  - PASS theo XML report:
    - `SheetValueMappersTest`: 9 tests, 0 failures, 0 errors.
    - `RepairSheetContractTest`: 2 tests, 0 failures, 0 errors.
- Full unit test:
  - PASS, khong co failure/error trong 18 XML test reports.
- Debug build:
  - `assembleDebug`: PASS / `BUILD SUCCESSFUL`.
- Secret guard:
  - Trong sandbox van fail ro voi `spawnSync C:\Windows\system32\cmd.exe EPERM`, dung hanh vi moi.
  - Quet thu cong file vua cham: khong thay secret/token that.

### Buoc tiep theo moi
- Lay/kiem tra header that cua sheet `Sua chua T4.2026`, chi doc metadata/header.
- Them push/pull repair sheet khong dung nham mapper DMBT.
