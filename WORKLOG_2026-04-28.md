# WORKLOG 2026-04-28

## Phạm vi
- Tiếp tục nhánh đồng bộ Google Sheets cho 2 sheet chính:
  - `DMBT 2026` (`gid=1607125070`)
  - `HGT định kỳ` (`gid=57428884`)

## Việc đã làm
1. Xác nhận preflight môi trường:
   - `node --version`
   - `npm --version`
   - `npx --version`
   - `git --version`
2. Rà soát luồng đồng bộ `Immediate sync` và xác định rủi ro hủy job do policy `REPLACE`.
3. Sửa scheduler để ổn định hơn:
   - File: `android-mvp/app/src/main/java/com/example/devicetracker/work/SyncScheduler.kt`
   - Đổi `ExistingWorkPolicy.REPLACE` -> `ExistingWorkPolicy.APPEND_OR_REPLACE`
   - Bỏ network constraint ở `scheduleImmediateSync()` để tránh kẹt hàng đợi khi trigger nhanh.
4. Chạy kiểm tra:
   - `testDebugUnitTest`: pass
   - `assembleDebug`: pass

## Kết quả
- Luồng enqueue đồng bộ tức thì an toàn hơn khi có nhiều thao tác thêm/sửa/xóa liên tiếp.
- APK debug đã build thành công tại:
  - `F:\codex_android_gsheet_full_pack\android-mvp\.codex-build\syncfix04\app\outputs\apk\debug\app-debug.apk`

## Vấn đề còn mở
- Từ môi trường sandbox hiện tại, lệnh `adb install` bị chặn do không ghi được profile path hệ thống (`C:\Users\CodexSandboxOnline\.android`).
- Cần user cài APK thủ công hoặc chạy lại ở môi trường có quyền ADB đầy đủ.

## Ghi chú vận hành
- Theo yêu cầu hiện tại: chưa dùng GSD cho runtime tới khi có mệnh lệnh mới.

---

## Cập nhật bổ sung (đợt syncfix05)

### Nguyên nhân lỗi người dùng gặp
- Màn `Trạng thái đồng bộ` hiển thị lỗi Google thô (`HTTP 401 invalid authentication credentials`), gây khó hiểu cho vận hành.

### Sửa đã thực hiện
1. Chuẩn hóa thông báo lỗi HTTP từ Google Sheets sang tiếng Việt, có hướng xử lý rõ:
   - `401`: token OAuth hết hạn/không hợp lệ.
   - `403`: thiếu quyền truy cập sheet/scope.
   - `404`: sai spreadsheetId hoặc sheetId.
2. Chuẩn hóa lại hiển thị lỗi ở `SyncStatusViewModel`:
   - Dù lỗi đến từ exception mới hay `latestQueueError` cũ trong DB, UI đều hiển thị thông điệp thân thiện.

### File thay đổi
- `android-mvp/app/src/main/java/com/example/devicetracker/data/remote/SheetsRemoteDataSource.kt`
- `android-mvp/app/src/main/java/com/example/devicetracker/ui/sync/SyncStatusViewModel.kt`

### Kết quả kiểm tra
- `testDebugUnitTest`: pass
- `assembleDebug`: pass
- APK mới:
  - `F:\codex_android_gsheet_full_pack\android-mvp\.codex-build\syncfix05\app\outputs\apk\debug\app-debug.apk`

### Cập nhật token + build mới (syncfix06)
- Đã thay token debug trong:
  - `android-mvp/local.properties`
- Build thành công APK mới:
  - `F:\codex_android_gsheet_full_pack\android-mvp\.codex-build\syncfix06\app\outputs\apk\debug\app-debug.apk`

### Tối ưu hiệu năng đồng bộ (syncfix07)
1. Thêm chế độ đồng bộ:
   - `Nhanh`: chỉ push pending, bỏ pull toàn bộ để chạy nhanh hơn.
   - `Đầy đủ`: push + pull toàn bộ để đối soát dữ liệu.
2. Tối ưu call API Google Sheets theo batch:
   - Batch update nhiều dòng trong một request.
   - Append nhiều dòng trong một request.
   - Delete nhiều dòng HGT trong một batchUpdate.
3. Chuẩn hóa thông báo thành công có thời gian thực thi (giây).
4. Đã cập nhật token mới theo yêu cầu user trong `local.properties`.
5. Build + cài trực tiếp lên điện thoại:
   - APK: `F:\codex_android_gsheet_full_pack\android-mvp\.codex-build\syncfix07\app\outputs\apk\debug\app-debug.apk`
   - ADB install: `Success`

### Khắc phục phản hồi UAT 4 lỗi (syncfix08)
1. Sửa thống kê `Trạng thái đồng bộ` để bao gồm cả dữ liệu HGT:
   - `total/synced/pending` giờ là tổng của `DeviceLog + HGT`.
2. Giảm lag nền:
   - `scheduleImmediateSync()` chuyển sang quick mode (push-only) qua inputData.
   - Worker chỉ pull toàn bộ khi chạy full sync (manual full hoặc periodic).
3. Làm rõ chế độ sync:
   - Hiển thị dòng `Đang chọn: Đồng bộ nhanh/đầy đủ`.
   - Đổi tên nút hành động theo mode (`Đồng bộ nhanh` / `Đồng bộ đầy đủ`).
4. Build + cài:
   - APK: `F:\codex_android_gsheet_full_pack\android-mvp\.codex-build\syncfix08\app\outputs\apk\debug\app-debug.apk`
   - ADB install: `Success`

## 2026-04-28 - Syncfix09: manual quick sync + pending device visibility
- Updated Google Sheets token in local.properties for debug build only; token is not copied into worklog.
- Stopped immediate auto-sync after add/update/delete actions so new local edits remain visible as PENDING until the user presses sync.
- Added pending sync item model and overview aggregation for DMBT logs, HGT checks, and queued HGT deletes.
- Added "Thiết bị chờ đồng bộ" section in Sync Status to show the exact devices waiting to sync.
- Kept periodic background sync scheduler unchanged; manual quick sync now pushes pending changes without pulling all sheets.
- Built APK with codexBuildId=syncfix09 and installed successfully to connected Android device.
